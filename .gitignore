# Dependencies
/node_modules

# Production
/build
/dist 

# Generated files
.docusaurus
.cache-loader

# Test artifacts (tests/e2e via playwright.config.ts)
/test-results
/playwright-report
/blob-report
/playwright/.cache

# Misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*
.dig

# gitnexus writes agent-tooling files into the repository it indexes. They are
# development-loop private and must never be tracked here.
# CLAUDE.md is intentionally NOT ignored: this repo authors its own operating notes.
# Always run `gitnexus analyze --skip-agents-md` here.
/AGENTS.md
/.claude/skills/gitnexus/
/.claude/skills/generated/
/.gitnexus/
